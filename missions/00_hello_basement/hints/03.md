# Hint 03

A good shape is:

```text
src/lib.zig   -> returns the greeting as []const u8
src/main.zig  -> imports lib.zig and prints the greeting
tests.zig     -> imports lib.zig and checks the greeting
```

This keeps the mission tiny while showing the pattern later missions will reuse.
