# Hint 02

The useful standard library call is:

```zig
std.debug.print("{s}", .{some_bytes});
```

The `{s}` placeholder formats a byte slice as a string. The second argument is an anonymous tuple of values to format.
