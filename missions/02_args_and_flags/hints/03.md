# Hint 03

For non-shout output, `std.fmt.bufPrint` can write directly into the caller's buffer.

For shout output, copy `"HELLO, "`, uppercase each name byte with `std.ascii.toUpper`, append `\n`, and return the slice of the buffer you filled.
