# Hint 02

Use `std.mem.eql(u8, arg, "--shout")` for exact flag matching and `std.mem.startsWith(u8, arg, "--")` for unknown flags.

Track the name as `?[]const u8` so you can detect both missing and duplicate names.
