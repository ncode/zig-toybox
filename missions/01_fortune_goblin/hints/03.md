# Hint 03

Use the remainder operator to wrap large indexes back into the array range:

```zig
const wrapped = index % fortunes.len;
```

Then return `fortunes[wrapped]`. No allocation is needed because the string literals live for the whole program.
