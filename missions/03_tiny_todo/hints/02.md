# Hint 02

The Zig 0.16 file helpers you want are on `std.Io.Dir`:

```zig
dir.readFileAlloc(io, path, allocator, .limited(64 * 1024))
dir.writeFile(io, .{ .sub_path = path, .data = bytes })
```

If reading returns `error.FileNotFound`, `listTodos` should allocate and return an empty string.
