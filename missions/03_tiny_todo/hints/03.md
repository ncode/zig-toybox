# Hint 03

For `removeTodo`, read the whole file, walk it line by line, and copy every line except the 1-based index being removed into a new buffer.

If index `0`, the file is missing, or the requested item was never found, return `error.InvalidIndex`.
